# CountryData

Analyzing data and performing unsupervised learning on `country data`. Using `pandas`, `matplotlib`, `seaborn`, `numpy`, `kmeans`, `hierarchial clustering`, and `tsne`. Finished report within `Country.ipynb`.